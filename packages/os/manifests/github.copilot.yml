name: GitHub Copilot
id: github.copilot
icon: "https://www.google.com/s2/favicons?domain=github.com/features/copilot&sz=128"
url: https://github.com/features/copilot
category: developer
ai_description: "AI coding assistant"
help_url: "https://docs.github.com/copilot"

navigation:
  overview: "GitHub Copilot — AI coding assistant Key features: chat interface (ask questions about code), inline suggestions (Tab to accept), slash commands (/explain /fix /tests /doc), context from open files, workspace mode"
  pages:
    main:
      description: "Main interface after loading."
      layout:
        - "Navigation at top or sidebar with main sections"
        - "Center content area"
        - "Action buttons for primary tasks"
  workflows:
    primary_action:
      - "Use the main navigation to find what you need"
      - "Search is usually available at the top"
      - "Click items to view details or interact"
  tips:
    - "Use keyboard shortcuts where available"
    - "Search is the fastest way to find content"
    - "chat interface (ask questions about code), inline suggestions (Tab to accept), slash commands (/explain /fix /tests /doc), context from open files, workspace mode"
